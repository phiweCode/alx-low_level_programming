#include "main.h"
#include <string.h>

/**
 * Description: _strcmp - compares two strings 
 * @s1: is a char pointer string param that is being compared
 * to.
 * @s2: is the string being compared
 * Returns: returns a integer value resulting from the comparison
 */

int _strcmp(char *s1, char *s2)
{
return (strcmp(s1 , s2 ));
}
